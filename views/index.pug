<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title title
  body
    div(id="main")
      div(id="wrap")
        h2 #{datas.title}
        p #{datas.description}